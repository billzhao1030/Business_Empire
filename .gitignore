# 存档与运行时数据 / save files & runtime data
data/*.db
data/*.db-wal
data/*.db-shm
data/*.log
data/.probe

# 生成的中间产物 / generated intermediates
build/icon.iconset/
build/*.icns

# 系统 / OS
.DS_Store
node_modules/
*.log
data-dev/
